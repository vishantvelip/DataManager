<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
  <title>Hire Vishant Velip – Full-Stack Developer</title>
  <!-- Font Awesome 6 Pro CDN (duotone icons) -->
  <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"/>
  <style>
    :root{
      --bg:#0c1624;
      --card:#111b2f;
      --primary:#3ea6ff;
      --accent:#ff63a5;
      --text:#e6e6e6;
      --muted:#adb7c2;
      --border:#22314d;
      --radius:18px;
    }
    *{box-sizing:border-box;margin:0;padding:0}
    body{
      font-family:Inter,Segoe UI,Arial,sans-serif;
      background:var(--bg);color:var(--text);line-height:1.6;
    }
    nav{display:flex;justify-content:space-between;align-items:center;padding:25px 7vw;font-size:1.1rem}
    .logo{font-weight:700;font-size:1.5rem;letter-spacing:.5px;color:#fff}
    .logo span{color:var(--primary)}
    .nav-links{display:flex;gap:30px}
    .nav-links a{color:#fff;text-decoration:none;font-weight:600}
    .nav-links a:hover{color:var(--primary)}
    main{max-width:960px;margin:0 auto;padding:40px 20px}
    section{background:var(--card);border:1px solid var(--border);border-radius:var(--radius);padding:40px;margin-bottom:40px;box-shadow:0 8px 30px rgba(0,0,0,.4)}
    h1,h2,h3{color:#fff;font-weight:700}
    h1{font-size:2.4rem;margin-bottom:10px}
    h2{color:var(--primary);font-size:1.3rem;margin-bottom:15px}
    .tech{display:flex;flex-wrap:wrap;gap:10px;margin-top:15px}
    .tech span{background:var(--primary);color:#fff;padding:6px 14px;border-radius:20px;font-size:.9rem;font-weight:600}
    .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:40px}
    .card{border:1px solid var(--border);border-radius:var(--radius);padding:30px;background:var(--card);transition:.3s}
    .card:hover{transform:translateY(-6px);box-shadow:0 0 20px var(--primary)}
    .price{font-size:2rem;color:var(--accent);font-weight:800;margin-bottom:10px}
    ul{list-style:none}
    ul li{margin-bottom:8px;padding-left:24px;position:relative}
    ul li::before{content:"\f00c";font-family:"Font Awesome 6 Free";font-weight:900;position:absolute;left:0;color:var(--primary)}
    .btn{display:inline-block;background:var(--primary);color:#fff;padding:12px 28px;border-radius:10px;font-weight:600;text-decoration:none;margin-top:15px;transition:.3s}
    .btn:hover{background:var(--accent);box-shadow:0 0 15px var(--accent)}
    label{display:block;margin:10px 0 5px;font-weight:600}
    select,input[type=email]{width:100%;padding:10px;border:1px solid var(--border);border-radius:6px;background:var(--bg);color:var(--text)}
    .checkbox-group label{display:flex;align-items:center;gap:8px;font-weight:400}
    .hidden{display:none}
    .modal-overlay{position:fixed;inset:0;background:rgba(0,0,0,.7);display:none;align-items:center;justify-content:center;z-index:999}
    .modal{background:var(--card);padding:35px;border-radius:var(--radius);max-width:400px;width:90%;position:relative}
    .close{position:absolute;top:15px;right:20px;font-size:1.5rem;cursor:pointer}
    footer{text-align:center;padding:30px;color:var(--muted)}
    .social a{color:var(--primary);margin:0 10px;font-size:1.3rem}

    /* duotone spinner styling */
    #loader{position:fixed;inset:0;background:#0c1624;display:flex;align-items:center;justify-content:center;z-index:9999;transition:opacity .6s}
    #loader.hide{opacity:0;pointer-events:none}
    #loader i{font-size:3rem;color:#3ea6ff;animation:spin 1.1s linear infinite}
    #loader p{color:#adb7c2;margin-top:15px;font-size:1.2rem}
    #loader span{color:#ff63a5}
    @keyframes spin{to{transform:rotate(360deg)}}
  </style>
</head>
<body>

<!-- Loading Screen (duotone spinner) -->
<div id="loader">
  <div>
    <i class="fa-duotone fa-solid fa-spinner"></i>
    <p>Loading… <span></span></p>
  </div>
</div>

<nav>
  <div class="logo">Vishant <span>Velip</span></div>
  <div class="nav-links">
    <a href="#services">Services</a>
    <a href="#calc">Calculator</a>
    <a href="#contact">Contact</a>
  </div>
</nav>

<main>
  <!-- Hero -->
  <section>
    <h1>👋 Hire Vishant Velip</h1>
    <h2>Full-Stack Web Developer</h2>
    <p>Turning ideas into fast, modern & scalable web apps. Let’s build something great together!</p>
    <div class="tech">
      <span>React / Next</span><span>Node / Express</span><span>Mongo / Postgres</span>
      <span>REST / GraphQL</span><span>UI/UX</span><span>SEO</span>
    </div>
  </section>

  <!-- Services -->
  <section id="services">
    <h2>Services & Rates</h2>
    <div class="grid">
      <div class="card">
        <h3>Hourly Billing</h3>
        <div class="price">₹500 / hr</div>
        <ul>
          <li>Static site – 15 hrs (₹7,500)</li>
          <li>Portfolio – 40 hrs (₹20,000)</li>
          <li>E-commerce – 120 hrs (₹60,000)</li>
          <li>Custom SaaS – 200+ hrs</li>
        </ul>
        <button class="btn" onclick="copyUpi()">Copy UPI ID</button>
        <span id="copied" style="color:var(--primary);margin-left:10px;display:none">Copied!</span>
      </div>

      <div class="card">
        <h3>What I Deliver</h3>
        <ul>
          <li>Responsive frontend (React / Next)</li>
          <li>Secure REST / GraphQL APIs</li>
          <li>Database design & integration</li>
          <li>SEO + performance tuning</li>
          <li>CI/CD & cloud deployment</li>
          <li>Bug fixes & rescue missions</li>
        </ul>
        <a href="#contact" class="btn">Get a Quote</a>
      </div>
    </div>
  </section>

  <!-- Calculator -->
  <section id="calc">
    <h2>Project Cost Estimator</h2>
    <label>Your email (for payment ref.)</label>
    <input type="email" id="payEmail" placeholder="you@example.com">

    <label>Type of website</label>
    <select id="siteType">
      <option value="0">Choose…</option>
      <option value="16">Static site – 16 h</option>
      <option value="40">Portfolio – 40 h</option>
      <option value="80">Blog – 80 h</option>
      <option value="120">E-commerce – 120 h</option>
    </select>

    <div class="checkbox-group">
      <label><input type="checkbox" value="12"> Responsive layout – 12 h</label>
      <label><input type="checkbox" value="8"> Contact form – 8 h</label>
      <label><input type="checkbox" value="20"> Admin panel – 20 h</label>
      <!-- NEW ₹1 Loading-Page Item -->
      <label><input type="checkbox" id="loadingPage" value="fixed:1"> Add custom loading page – ₹1</label>
    </div>

    <p style="font-size:1.2rem;margin-top:20px">
      Total: <span id="totalHrs">0</span> h × ₹500 + ₹<span id="fixedCost">0</span> = ₹<span id="totalFee">0</span>
    </p>
  </section>

  <!-- Contact -->
  <section id="contact">
    <h2>Get in Touch</h2>
    <form id="contactForm">
      <label>Name</label>
      <input type="text" name="name" required>

      <label>Email</label>
      <input type="email" name="email" required>

    <label>Project details</label>
<textarea name="msg" rows="8" style="min-height:280px; width:500px; max-width:100%;" required
          placeholder="Describe your project, features, timeline, budget, etc."></textarea>
      <button class="btn" type="submit">Send & Pay</button>
    </form>
  </section>
</main>

<!-- Payment modal -->
<div class="modal-overlay" id="modal">
  <div class="modal">
    <span class="close" onclick="closeModal()">&times;</span>
    <h3>Complete Payment</h3>
    <p>Amount: ₹<span id="finalFee">0</span></p>
    <p>UPI: <strong>9420970930@ibl</strong></p>
    <p style="font-size:.9rem;color:var(--muted)">Add your email in the UPI note.</p>
    <a class="btn" id="upiLink" href="#">Pay with any UPI app</a>
  </div>
</div>

<footer>
  <div class="social">
    <a href="https://github.com/vishantvelip" target="_blank"><i class="fab fa-github"></i></a>
    <a href="https://linkedin.com/in/vishantvelip" target="_blank"><i class="fab fa-linkedin"></i></a>
    <a href="mailto:vishantvelip92@gmail.com"><i class="fas fa-envelope"></i></a>
  </div>
  © 2025 Vishant Velip
</footer>

<script>
const hourlyRate = 500;
const upiId = '9420970930@ibl';

/* ---- calculator ---- */
function calc(){
  let h = +document.getElementById('siteType').value;
  let fixed = 0;

  document.querySelectorAll('input[type=checkbox]:checked').forEach(ch=>{
    if (ch.value.startsWith('fixed:')) {
      fixed += +ch.value.split(':')[1];
    } else {
      h += +ch.value;
    }
  });

  const hoursCost = h * hourlyRate;
  const total = hoursCost + fixed;

  document.getElementById('totalHrs').textContent = h;
  document.getElementById('fixedCost').textContent = fixed;
  document.getElementById('totalFee').textContent = total.toLocaleString('en-IN');
}
document.getElementById('siteType').addEventListener('change',calc);
document.querySelectorAll('input[type=checkbox]').forEach(ch=>ch.addEventListener('change',calc));

/* ---- contact form ---- */
document.getElementById('contactForm').addEventListener('submit',e=>{
  e.preventDefault();
  const email = e.target.email.value.trim();
  const fee = +document.getElementById('totalFee').textContent.replace(/,/g,'');
  openModal(email,fee);
});

/* ---- modal ---- */
function openModal(email,fee){
  document.getElementById('finalFee').textContent = fee.toLocaleString('en-IN');
  const link = `upi://pay?pa=${upiId}&pn=VishantVelip&tn=ProjectPayment-${email}&am=${fee}&cu=INR`;
  document.getElementById('upiLink').href = link;
  document.getElementById('modal').style.display='flex';
}
function closeModal(){document.getElementById('modal').style.display='none'}

/* ---- copy UPI ---- */
function copyUpi(){
  navigator.clipboard.writeText(upiId);
  const c=document.getElementById('copied');
  c.style.display='inline';
  setTimeout(()=>c.style.display='none',2000);
}

/* ---- ₹1 loader hide after 1.5s ---- */
window.addEventListener('load',()=>{
  setTimeout(()=>{
    const loader=document.getElementById('loader');
    loader.classList.add('hide');
    setTimeout(()=>loader.remove(),600);
  },1500);
});

calc();   // init
</script>
</body>
</html>
